# Comments System (mini-YouTube Comments)

## 📝 Цель проекта

Создать веб-приложение **Comments System** — систему комментариев как на YouTube, но упрощённую. В этом проекте вы отработаете:

- **DOM API** — создание элементов, события, манипуляция страницей без фреймворков
- **Обработка событий** — формы, кнопки, обработчики кликов
- **Структуры данных** — вложенные массивы и объекты для комментариев и ответов
- **Чистые функции** — не мутировать входные данные
- **Точечные обновления DOM** — не пересоздавать всю страницу, обновлять только нужные части

**Ключевая идея:** комментарии и ответы хранятся в JavaScript объектах в памяти браузера. При взаимодействии пользователя — обновляем данные и синхронизируем с DOM.

---

## 📁 Структура проекта

```
mini-YouTube-Comments/
  src/
    index.html            # страница приложения
    styles.css            # стили
    main.js               # точка входа, обработка событий
    comments.js           # функции для работы с комментариями
    render.js             # функции для создания DOM элементов
    utils.js              # вспомогательные функции (генерация id)
  package.json
  README.md
```

---

## 📦 Модель данных

**Комментарий:**
```javascript
{
  id: number,           // уникальный идентификатор
  text: string,         // текст комментария
  likes: number,        // количество лайков
  replies: array        // массив ответов (также объекты)
}
```

**Ответ (Reply):**
```javascript
{
  id: number,           // уникальный идентификатор
  parentId: number,     // id комментария, на который отвечаем
  text: string,         // текст ответа
  likes: number         // количество лайков
}
```

**Пример данных:**
```javascript
const comments = [
  {
    id: 1,
    text: "Отличное видео!",
    likes: 5,
    replies: [
      { id: 101, parentId: 1, text: "Согласен!", likes: 2 },
      { id: 102, parentId: 1, text: "Спасибо!", likes: 1 }
    ]
  },
  {
    id: 2,
    text: "Можешь ещё такое?",
    likes: 3,
    replies: []
  }
];
```

---

## 🎯 Задачи

### 1️⃣ `createComment`

**Что нужно сделать:** Создать новый объект комментария с уникальным `id`.

**Сигнатура:**
```javascript
const createComment = (text) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
createComment("Отличное видео!")
// → {
//     id: 1,
//     text: "Отличное видео!",
//     likes: 0,
//     replies: []
//   }

createComment("Спасибо!")
// → {
//     id: 2,
//     text: "Спасибо!",
//     likes: 0,
//     replies: []
//   }
```

**Подсказка:**
- Генерируйте `id` с помощью `Date.now()` или счётчика
- Возвращайте новый объект, не мутируйте входные данные
- `likes` всегда начинается с 0
- `replies` всегда пустой массив для нового комментария

---

### 2️⃣ `createReply`

**Что нужно сделать:** Создать объект ответа, который привязан к конкретному комментарию.

**Сигнатура:**
```javascript
const createReply = (parentId, text) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
createReply(1, "Согласен!")
// → {
//     id: 101,
//     parentId: 1,
//     text: "Согласен!",
//     likes: 0
//   }

createReply(1, "Спасибо за ответ!")
// → {
//     id: 102,
//     parentId: 1,
//     text: "Спасибо за ответ!",
//     likes: 0
//   }
```

**Подсказка:**
- `parentId` — это `id` комментария, на который отвечаем
- Ответ НЕ имеет массива `replies` (можно отвечать только на комментарии)
- Генерируйте `id` так же как для комментариев

---

### 3️⃣ `likeComment`

**Что нужно сделать:** Увеличить счётчик лайков на 1, вернув новый объект.

**Сигнатура:**
```javascript
const likeComment = (comment) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
const comment = { id: 1, text: "Text", likes: 5, replies: [] };

likeComment(comment)
// → { id: 1, text: "Text", likes: 6, replies: [] }

console.log(comment.likes)  // 5 (оригинал не изменился!)
```

**Подсказка:**
- Создайте новый объект со спред-оператором
- Увеличьте `likes` на 1: `{ ...comment, likes: comment.likes + 1 }`
- Исходный объект должен остаться без изменений

---

### 4️⃣ `deleteComment`

**Что нужно сделать:** Удалить комментарий по `id` и все его ответы.

**Сигнатура:**
```javascript
const deleteComment = (comments, id) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
const comments = [
  { id: 1, text: "First", likes: 0, replies: [
    { id: 101, parentId: 1, text: "Reply", likes: 0 }
  ]},
  { id: 2, text: "Second", likes: 0, replies: [] }
];

deleteComment(comments, 1)
// → [
//     { id: 2, text: "Second", likes: 0, replies: [] }
//   ]
// Комментарий 1 и его ответ (101) удалены

deleteComment(comments, 2)
// → [
//     { id: 1, text: "First", likes: 0, replies: [...] }
//   ]

console.log(comments)  // исходный массив не изменился
```

**Подсказка:**
- Используйте `.filter()` для создания нового массива без комментария
- Вложенные ответы удаляются вместе с комментарием
- НЕ мутируйте исходный массив

---

### 5️⃣ `renderComment`

**Что нужно сделать:** Создать DOM элемент для отображения одного комментария.

**Структура элемента:**
```
<div class="comment">
  <p class="comment-text">Текст комментария</p>
  <div class="comment-actions">
    <button class="like-btn">👍 Лайк (5)</button>
    <button class="reply-btn">Ответить</button>
    <button class="delete-btn">Удалить</button>
  </div>
  <div class="replies">
    <!-- Сюда добавляются ответы -->
  </div>
  <div class="reply-form" style="display: none;">
    <!-- Форма для ответа (добавляется при клике "Ответить") -->
  </div>
</div>
```

**Сигнатура:**
```javascript
const renderComment = (comment) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
const comment = {
  id: 1,
  text: "Отличное видео!",
  likes: 5,
  replies: []
};

const element = renderComment(comment);
// → <div class="comment"> с текстом, кнопками и контейнером для ответов

document.querySelector('.comments-list').appendChild(element);
```

**Подсказка:**
- Используйте `document.createElement()` для создания элементов
- Используйте `appendChild()` для добавления элементов внутрь
- Используйте `addEventListener()` для обработки кликов
- Не используйте `innerHTML` (небезопасно)
- Добавьте атрибуты `data-id` для идентификации элементов
- Класс `.replies` — контейнер для ответов (изначально может быть пуст)

---

### 6️⃣ `renderReply`

**Что нужно сделать:** Создать DOM элемент для отображения одного ответа.

**Структура элемента:**
```
<div class="reply">
  <p class="reply-text">Текст ответа</p>
  <div class="reply-actions">
    <button class="like-btn">👍 Лайк (2)</button>
    <button class="delete-btn">Удалить</button>
  </div>
</div>
```

**Сигнатура:**
```javascript
const renderReply = (reply) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
const reply = {
  id: 101,
  parentId: 1,
  text: "Согласен!",
  likes: 2
};

const element = renderReply(reply);
// → <div class="reply"> с текстом и кнопками

document.querySelector('.replies').appendChild(element);
```

**Подсказка:**
- Похоже на `renderComment`, но проще (нет вложенности, нет кнопки "Ответить")
- Используйте те же методы DOM API

---

### 7️⃣ `main.js` интеграция

**Что нужно сделать:**
1. Обработать форму добавления комментариев
2. Обработать кнопки лайка, ответа, удаления
3. Обновлять данные и DOM синхронно

**Логика работы:**

```
1. Пользователь вводит текст и отправляет форму
   ↓
2. Создаём новый комментарий через createComment()
3. Добавляем в массив comments
4. Рендерим элемент через renderComment()
5. Добавляем в DOM

6. При клике "Лайк"
   ↓
7. Обновляем объект через likeComment()
8. Обновляем счётчик в DOM (локально)

9. При клике "Удалить"
   ↓
10. Удаляем из массива через deleteComment()
11. Удаляем элемент из DOM
```

**Подсказка:**
- Используйте `addEventListener()` для обработки событий
- Используйте делегирование событий или обработчики для каждой кнопки
- Обновляйте только нужную часть DOM, не пересоздавайте список
- Используйте `event.target` для определения, на какую кнопку кликнули

---

## 🛠️ Общие требования

### Синтаксис JavaScript

- ✅ **ИСПОЛЬЗУЙТЕ:** `const` и `let`
- ✅ **ИСПОЛЬЗУЙТЕ:** стрелочные функции `() => {}`
- ✅ **ИСПОЛЬЗУЙТЕ:** методы массивов: `.filter()`, `.map()`, `.find()`
- ✅ **ИСПОЛЬЗУЙТЕ:** спред-оператор `{ ...obj }`
- ❌ **НЕ ИСПОЛЬЗУЙТЕ:** `var`
- ❌ **НЕ ИСПОЛЬЗУЙТЕ:** `innerHTML` (небезопасно)
- ❌ **НЕ ИСПОЛЬЗУЙТЕ:** внешние библиотеки

### DOM API

- ✅ **ИСПОЛЬЗУЙТЕ:** `document.createElement()`
- ✅ **ИСПОЛЬЗУЙТЕ:** `element.appendChild()`
- ✅ **ИСПОЛЬЗУЙТЕ:** `element.addEventListener()`
- ✅ **ИСПОЛЬЗУЙТЕ:** `element.classList.add/remove`
- ✅ **ИСПОЛЬЗУЙТЕ:** `element.setAttribute()`
- ✅ **ИСПОЛЬЗУЙТЕ:** `element.textContent`
- ❌ **НЕ ИСПОЛЬЗУЙТЕ:** `element.innerHTML`
- ❌ **НЕ ИСПОЛЬЗУЙТЕ:** `element.onclick = ...` (старый синтаксис)

---

## 💡 Полезные советы для новичков

### Создание элементов в DOM

```javascript
// Создать элемент
const div = document.createElement('div');
const button = document.createElement('button');

// Добавить текст
button.textContent = 'Нажми меня';

// Добавить класс
div.classList.add('my-class');
div.classList.add('another-class');

// Добавить атрибут
div.setAttribute('data-id', '123');
div.setAttribute('data-type', 'comment');

// Добавить элемент внутрь другого
parent.appendChild(div);

// Вложенность
const comment = document.createElement('div');
const text = document.createElement('p');
const button = document.createElement('button');
text.textContent = 'Текст комментария';
button.textContent = 'Лайк';
comment.appendChild(text);
comment.appendChild(button);
```

### Обработка событий

```javascript
const button = document.querySelector('.like-btn');

// Добавить обработчик события
button.addEventListener('click', (event) => {
  console.log('Кнопка нажата!');
  console.log(event.target);  // Элемент, на который кликнули
});

// Обработка формы
const form = document.querySelector('#comment-form');
form.addEventListener('submit', (event) => {
  event.preventDefault();  // Предотвратить перезагрузку страницы
  const input = form.querySelector('input');
  const text = input.value;
  console.log('Текст:', text);
  input.value = '';  // Очистить поле
});
```

### Поиск элементов в DOM

```javascript
// Найти первый элемент
const element = document.querySelector('.my-class');

// Найти все элементы
const elements = document.querySelectorAll('.comment');

// Найти по id
const element = document.getElementById('main');

// Найти внутри элемента
const parent = document.querySelector('.comments-list');
const child = parent.querySelector('.comment');
```

### Удаление элементов

```javascript
const element = document.querySelector('.comment');

// Удалить элемент из DOM
element.remove();

// Или удалить через родителя
element.parentElement.removeChild(element);
```

### Атрибуты data-*

```javascript
// Добавить атрибут
const div = document.createElement('div');
div.setAttribute('data-id', '123');
div.setAttribute('data-type', 'comment');

// Получить атрибут
const id = div.getAttribute('data-id');  // '123'
const type = div.getAttribute('data-type');  // 'comment'

// В CSS
// div[data-type="comment"] { ... }
```

### Точечные обновления DOM

```javascript
// ❌ Пересоздавать весь список (неэффективно)
const list = document.querySelector('.comments-list');
list.innerHTML = '';
comments.forEach(comment => {
  const el = renderComment(comment);
  list.appendChild(el);
});

// ✅ Обновить только нужный элемент (эффективно)
const commentEl = document.querySelector(`[data-id="${id}"]`);
const likeBtn = commentEl.querySelector('.like-btn');
likeBtn.textContent = `👍 Лайк (${newLikes})`;
```

---

## 🎓 Что вы узнаете, делая этот проект

✅ Как работает **DOM API** и создание элементов  
✅ Как обрабатывать **события** (клики, отправка формы)  
✅ Как работать с **вложенными структурами данных**  
✅ Как писать **чистые функции** без побочных эффектов  
✅ Как делать **точечные обновления DOM** (не пересоздавать всё)  
✅ Как организовать **взаимодействие между JavaScript и страницей**  
✅ Как делать **веб-приложения без фреймворков**  

**Успехов! 🚀**
